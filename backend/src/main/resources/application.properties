spring.application.name=backend

<<<<<<< HEAD
# =============================================================
# DATABASE CONFIGURATION
# =============================================================
# In production set SPRING_DATASOURCE_URL / USERNAME / PASSWORD via env.
# A blank URL falls back to the local profile (application-local.properties).
spring.datasource.url=${SPRING_DATASOURCE_URL:}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}

spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# =============================================================
# SERVER CONFIGURATION
# =============================================================
=======
# =========================
# DATABASE CONFIGURATION
# =========================

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# =========================
# SERVER CONFIGURATION
# =========================

>>>>>>> e43aea6 (update frontend api config)
server.port=${PORT:8080}
server.servlet.context-path=/

<<<<<<< HEAD
# =============================================================
# AI SERVICE
# =============================================================
ai.service.url=${AI_SERVICE_URL:http://localhost:8000}
roadwatch.ai.optional=${ROADWATCH_AI_OPTIONAL:true}

# =============================================================
# FILE UPLOAD CONFIGURATION
# =============================================================
roadwatch.uploads.dir=${ROADWATCH_UPLOADS_DIR:uploads}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# =============================================================
# PUBLIC BASE URL (used for generating image URLs)
# =============================================================
roadwatch.public-base-url=${ROADWATCH_PUBLIC_BASE_URL:http://localhost:8080}

# =============================================================
# ADMIN CREDENTIALS
# =============================================================
roadwatch.admin.username=${ADMIN_USERNAME:admin}
roadwatch.admin.password=${ADMIN_PASSWORD:admin123}

# =============================================================
# JWT CONFIGURATION
# =============================================================
roadwatch.jwt.secret=${JWT_SECRET:roadwatch-jwt-secret-change-me-in-production-min-32-chars}
roadwatch.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
=======
# =========================
# AI SERVICE
# =========================

ai.service.url=${AI_SERVICE_URL:http://localhost:8000}
roadwatch.ai.optional=true

# =========================
# FILE UPLOAD CONFIGURATION
# =========================

roadwatch.uploads.dir=${UPLOAD_DIR:uploads}

# =========================
# PUBLIC BASE URL
# =========================

roadwatch.public-base-url=${PUBLIC_BASE_URL}

# =========================
# ADMIN CONFIG
# =========================

roadwatch.admin.username=${ADMIN_USERNAME:admin}
roadwatch.admin.password=${ADMIN_PASSWORD:admin123}

# =========================
# JWT CONFIGURATION
# =========================

roadwatch.jwt.secret=${JWT_SECRET}
roadwatch.jwt.expiration-ms=86400000

# =========================
# CORS CONFIGURATION
# =========================

roadwatch.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,https://roadwatch-olive.vercel.app,https://github.dev,https://*.github.dev,https://*.app.github.dev}

# =========================
# FILE SIZE
# =========================

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# =========================
# JACKSON CONFIG
# =========================
>>>>>>> e43aea6 (update frontend api config)

# =============================================================
# CORS CONFIGURATION
# =============================================================
roadwatch.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,https://*.vercel.app}

# =============================================================
# JACKSON CONFIGURATION
# =============================================================
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss
spring.jackson.time-zone=Asia/Kolkata
<<<<<<< HEAD

# =============================================================
# ACTUATOR (health check for AWS / load balancers)
# =============================================================
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when-authorized
management.endpoint.health.probes.enabled=true
management.info.env.enabled=true
info.app.name=RoadWatch Governance API
info.app.version=2.0.0
info.app.description=National Road Governance, Transparency and Accountability Platform

# =============================================================
# SECURITY / RATE LIMIT
# =============================================================
roadwatch.security.rate-limit.enabled=${ROADWATCH_RATE_LIMIT_ENABLED:true}
roadwatch.security.rate-limit.writes-per-minute=${ROADWATCH_RATE_LIMIT_WRITES_PER_MINUTE:60}

# =============================================================
# SWAGGER / OPENAPI
# =============================================================
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.api-docs.path=/v3/api-docs

# =============================================================
# LOGGING
# =============================================================
=======
>>>>>>> e43aea6 (update frontend api config)
logging.level.org.hibernate.orm.jdbc.bind=WARN
logging.level.com.roadwatch=INFO
logging.level.AUDIT=INFO
logging.pattern.level=%5p [rid=%X{requestId:-}]
