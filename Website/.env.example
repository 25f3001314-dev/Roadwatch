<<<<<<< HEAD
# RoadWatch Admin — environment variables.
#
# Copy this file to `.env.local` for local development.
#
# VITE_API_BASE_URL is the absolute origin of the Spring Boot API.
# - Local dev:  http://localhost:8080  (matches docker-compose)
# - Production: http://16.176.208.7:8080  (AWS EC2 backend)
=======
# Backend API URL for production deployments
# When the UI is deployed separately from the backend, set this to your AWS backend URL.
# For local development, set this only when your browser can reach localhost:8080 directly.
# In remote preview environments (GitHub.dev / Codespaces), leave this blank to use Vite's /api proxy.
>>>>>>> e43aea6 (update frontend api config)
VITE_API_BASE_URL=http://localhost:8080
